# Dev server must stay running in terminal (npm start). Refresh fails if you close it.
HOST=0.0.0.0
PORT=3000
WDS_SOCKET_HOST=0.0.0.0
DANGEROUSLY_DISABLE_HOST_CHECK=true

# API: auto-uses same host as browser (see src/api/config.js). Override if needed:
# REACT_APP_API_URL=http://localhost:8080
